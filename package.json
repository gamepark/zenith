{
  "private": true,
  "workspaces": [
    "rules",
    "app"
  ],
  "author": "Romain Fromi <romain@game-park.com> (https://game-park.com/)",
  "license": "ISC",
  "scripts": {
    "dev": "yarn workspace @gamepark/zenith-app run dev",
    "test": "yarn workspace @gamepark/zenith run test && yarn workspace @gamepark/zenith-app run test",
    "preview": "yarn workspace @gamepark/zenith-app run preview",
    "build": "yarn workspace @gamepark/zenith-app run build",
    "deploy": "yarn workspace @gamepark/zenith-app run deploy",
    "publish": "yarn workspace @gamepark/zenith run publish",
    "lint": "yarn workspace @gamepark/zenith run lint && yarn workspace @gamepark/zenith-app run lint",
    "linkgp": "yarn link ../rules-api ../react-client ../react-game -r && git update-index --skip-worktree package.json yarn.lock",
    "unlinkgp": "git update-index --no-skip-worktree package.json yarn.lock && yarn unlink ../react-game ../react-client ../rules-api"
  },
  "packageManager": "yarn@4.18.0",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@typescript/native": "npm:typescript@^7.0.2",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.6",
    "globals": "^17.9.0",
    "prettier": "^3.9.6",
    "typescript": "npm:@typescript/typescript6@^6.0.2",
    "typescript-eslint": "^8.66.0",
    "vitest": "^4.1.10"
  }
}
